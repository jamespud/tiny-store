# Runbook：预占 TTL 扫尾积压

## 症状
- `sweeper_lag` 持续上升；大量 Reservation 超时未释放。

## 处置
- 临时提高扫尾并发与批量；
- 优先处理最早过期分区，必要时按 SKU 热点优先级；
- 与 Orchestrator 协同，冻结新下单的支付窗口（防止新悬挂）；
- 观察库存读模型与订单取消量，必要时降级销售（关闭热 SKU 下单）。

## 验收
- `sweeper_lag` 恢复至阈值内；库存一致性校验通过；无超额释放。
